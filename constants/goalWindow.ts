// Length of a goal's active window, measured from when the user selected
// it (user_goals.created_at) — shared by the Goals progress bar and the
// objective day-checkbox disabling so they always agree on the boundary.
export const GOAL_WINDOW_WEEKS = 12;
